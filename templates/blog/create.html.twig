{% extends 'base.html.twig' %}


{% block body %}


<h1>

{% if editMode %}
Modif de l'article n° {{editMode}}
{% else %}

Creation d'un nouvel article

{% endif %}


</h1>

{#<form>
  <div class="form-row" method="post">
    <div class="form-group col-md-6">
      <label for="text">Titre</label>
      <input name="title" type="text" class="form-control" id="inputEmail4">
    </div>
    <div class="form-group col-md-6">
      <label for="text">Contenu</label>
      <input name="content" type="text" class="form-control" id="inputPassword4">
    </div>
  </div>
  <div class="form-group">
    <label for="inputAddress">Image</label>
    <input name"image" type="text" class="form-control" id="inputAddress" >
  </div>
 
  <button type="submit" class="btn btn-primary">Sign in</button>
</form>#}


{#{{ form(formArticle) }}#}

{{ form_start(formArticle, {'attr': {'class': 'col-md-5 mx-auto'} }) }}

    {{ form_row(formArticle.title, {'attr': {'placeholder': "saisir ceci"}, 'label': "Titre de l'article"}) }}

      {{ form_row(formArticle.content, {
        'attr': {'placeholder': "contenu de l'article",
        'rows' : 15
        },
        'label': "Contenu de l'article"
      }) }}

        {{ form_row(formArticle.image, {
          'attr': {'placeholder': "Saisir l'URL de l'image"},
          'label': "URL de l'image"
        }) }}

          <button type="submit">Enregistrer l'article</button>



{{ form_end(formArticle) }}

{% endblock %}